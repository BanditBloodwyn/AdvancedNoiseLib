<Window x:Class="AdvancedNoiseLib_Studio.Controls.Settings.NoiseFilterChoiceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AdvancedNoiseLib_Studio.Controls.Settings"
        mc:Ignorable="d"
        Title="NoiseFilterChoiceWindow" Height="200" Width="300" Background="#1e1e1e" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <ComboBox Grid.Row="0" x:Name="cmb_Choice" Margin="20" Height="25"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Cancel" Margin="10" Click="Cancel_Click"/>
            <Button Grid.Column="1" Content="Ok" Margin="10" Click="OK_Click"/>
        </Grid>
    </Grid>
</Window>
